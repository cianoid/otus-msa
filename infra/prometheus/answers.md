### Что фиксирует график "HTTP Status 500 test"? 
Там запрос такой: `sum(http_requests_total{handler="/test", status="500", service="hw4-app"})`

Дело в том, что в ДЗ написано странно "Error Rate - количество 500ых ответов". То есть у нас тут "количество" или все-таки "Rate"? Я сделал количество, но теперь понимаю, что это не показательная штука и нужно делать именно Rate

### Что обозначают желтые, красные, голубые вертикальные прерывистые линии?
Это срабатывание алертов в Alert Manager

## Если обратиться к promQL из json sum(http_requests_total{handler=\"/test\", status=\"500\", service=\"hw4-app\"}) то тут фиксируется только общий counter а не rate в еденицу времени, по идее такой график должен просто всегда расти. 
Все верно, ответ выше

### Если я правильно понимаю, то в данном задании вы написали сервис, с одним методом который рандомно возвращает 200/500 статусы, как показывал лектор, предлагаю изменить подход, и использовать АПИ уже разработанного приложения из предыдущих дз (user-service) и сделать метрики например на create user.
Я сделаю, но надо понимать, что тогда 500 ошибку поймать будет невозможно, потому что сервис будет просто создавать пользователя и таких задержек не будет, так как метод отрабатывает очень быстро

### Как был получен такой график по RPS /test/? Просто запустили нагрузку рандомную или все же добавляли искусственные задержки в реализацию метода /test/?
Как запускал нагрузку, описывал [здесь](https://github.com/cianoid/otus-msa/blob/8c0d8e9b1455f45afa238f5ead14ad9ac7fd5edd/hw5-prometheus/readme.md#%D0%B7%D0%B0%D0%BF%D1%83%D1%81%D1%82%D0%B8%D1%82%D1%8C-%D0%BD%D0%B0%D0%B3%D1%80%D1%83%D0%B7%D0%BE%D1%87%D0%BD%D0%BE%D0%B5-%D1%82%D0%B5%D1%81%D1%82%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D0%B5)

Нагрузка генерировалась обычными математическими вычислениями, описанными в [этом участке кода](https://github.com/cianoid/otus-msa/blob/8c0d8e9b1455f45afa238f5ead14ad9ac7fd5edd/hw4-helm/app/api.py#L76_L78)

### Latency p50/p95/p99 Это график который называется Квантили 0.5,0.95, 0.99? Давайте дадим читаемые название в легенде к этому графику

Готово

### Часть графиков названо в англоязычной манере со смайликами, часть в русскоязычной, желательно привести к единому виду.
Это не смайлики :D Это сработавшие алерты. Если что, подтянулась `Grafana v13.1.0 (b309c9bb3b)`

### так же, например, в графике RPS /test/ нет легенды и подразумевается в названии графика то что измеряется, а в графике Среднее время выполнения запроса есть легенда.

### Не увидел настроенных алертов.
Про алерты написал выше. Дополнительно добавил скриншоты настроек алертов и сами алерты, которые ушли в телегу

### не увидел latency (max)
Добавил
